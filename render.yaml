services:
  - type: web
    name: forensiq-detector
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --timeout 120 --preload app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.2
